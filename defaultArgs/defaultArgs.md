1.Keita Odaさんのコメント
```python
これは、def で関数が定義されたときに、一回だけ、引数のデフォルト値が評価されます。

def f(i, a=[]):
  a.append(i)
  return a

print(f(5))
print(f(3))
print(f(1))


これは何が起きるでしょうか。
```

takasaanさんのコメント
```python

def で関数が定義されたとき　1回だけ、引数のデフォルト値が評価される


def f(i, a=[]): #デフォルト引数の f　に、 i と　a=[] aのリストが設定される
  a.append(i) #appendメソッドで、aのリストの最後に i が追加される
  return a　#aのリストが更新される

print(f(5))
print(f(3))
print(f(1))
なので、出力結果は

[5]
[3]
[1] 
???
あ違いました
def で関数が定義されたとき　1回だけ、引数のデフォルト値が評価される

この意味がちゃんと理解できてませんでした。
a=[]のリストに追加される（再利用される）ので、
[5]
[5,3]
[5,3,1]
  となることがわかりました
  ```
